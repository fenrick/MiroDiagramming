# Node Architecture (Reference Only)

The backend was fully removed; keep this file only to note that any remaining references to Fastify/Prisma or `/api/*` calls should be deleted on sight. All new work belongs in `docs/architecture/ARCHITECTURE.md` and the modules/UX/operations folders.
